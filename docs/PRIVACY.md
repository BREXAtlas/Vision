# Privacy

All profile, journal, financial-scenario, project, and progress data stays in browser localStorage unless the learner exports a JSON or Markdown file. The app has no backend, analytics, tracking pixel, or runtime API. Imports are validated and malformed data is rejected without replacing current state. Clearing browser storage removes local data.
