# Maintenance

Update official URLs and review dates periodically. Add content through typed libraries, keep all quiz explanations, and run `npm run validate:content`. Increment persisted-state versions only with a migration path. Test keyboard, mobile, reduced-motion, import rejection, and the full Pages base path before release.
